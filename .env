# Port to listen on (example: 3000)
PORT=3000

# MongoDB database URL (example: mongodb://localhost/dbname)
DATABASE_URL=mongodb+srv://user:user@cluster0.8wuam9k.mongodb.net/Cluster0?retryWrites=true&w=majority

# Session secret string (must be unique to your server)
SESSION_SECRET=8b196850d6284c1284d4595cc5df436b

# Set to '1' to enable TLS certificate verification, '0' to disable (not recommended for production)
NODE_TLS_REJECT_UNAUTHORIZED=0  // INPUT_REQUIRED {Set to '0' only if necessary for local development with self-signed certificates}